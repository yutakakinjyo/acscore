<h1>Home</h1>

<p>
  <%= @user.email %>
</p>

<%= render 'form' %>

<table>
  <td>
    <% if !@scores.nil? %>
      <%= @now.strftime("%Y/%m/%d %X") %>
    <% end %>
  </td>
</table>

<table>
<tr>
  <% @da.each do |d| %>
    <td>
      <%= d.strftime("%d")%>
    </td>
  <% end %>
</tr>
</table>

<table>
<tr>
  <td>
    <% if !@scores.nil? %>
      <%= @scores.size %>
    <% end %>
  </td>
</tr>
  <% @scores.each do |s| %>
<tr>
    <td>
      <%= s.created_at.strftime("%Y/%m/%d %X") %>
    </td>
</tr>
  <% end %>
</table>

